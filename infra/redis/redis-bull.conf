# Redis — BullMQ instance
# Durability is required: AOF enabled, no eviction
bind 0.0.0.0
protected-mode no
appendonly yes
appendfsync everysec
maxmemory-policy noeviction
save 900 1
save 300 10
save 60 10000
